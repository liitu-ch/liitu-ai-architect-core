---
description: Create Vitest unit tests for domain logic, mappers, components, and architecture checks
---

Use the `vitest` skill from the `ai-architect-testing` plugin to create Vitest unit tests for React components, domain logic, data mappers, and architecture/layer-boundary checks using `@testing-library/react` with accessibility-first queries.

$ARGUMENTS
