---
description: Create Playwright E2E tests for React views with accessibility-first locators
---

Use the `playwright-test` skill from the `ai-architect-testing` plugin to create Playwright end-to-end tests for React views using `@playwright/test` with accessibility-first locators (`getByRole`, `getByLabel`, `getByText`).

$ARGUMENTS
